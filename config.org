#+TITLE:  Emacs Configuration File
#+AUTHOR: Julien Blanchard
#+EMAIL:  julien@typed-hole.org
#+PROPERTY: header-args :results silent
#+PROPERTY: header-args:sh  :tangle no
#+PROPERTY: header-args :tangle ~/.emacs.d/init.el
#+PROPERTY: header-args :eval no-export
#+PROPERTY: header-args :comments org
#+STARTUP: overview

#+begin_src emacs-lisp :comments no :padline no
;; -*- lexical-binding: t -*-
#+end_src

* Emacs Initialization

This section contains the basic commands you need to know to properly initialize
your GNU Emacs.

#+begin_src emacs-lisp
  (defvar as/init-dir "~/.emacs.d")

  ;; this is where to put elisp libraries from the Internet (including
  ;; my own)
  (add-to-list 'load-path (expand-file-name "lib" as/init-dir))

  (setq custom-file "~/.emacs.d/custom.el")
  (load custom-file)

  (setq create-lockfiles nil)
#+end_src

Require netrc to read secrets from ~/.netrc

#+begin_src emacs-lisp
  (require 'netrc)
#+end_src

* Packages config

#+begin_src emacs-lisp
  (require 'use-package)

  ;; Ensure all the packages we "use" are installed
  (require 'use-package-ensure)
  (setq use-package-always-ensure t)

  (use-package delight)
#+end_src

* Path

Use exec-path-from-shell

#+begin_src emacs-lisp
  (use-package exec-path-from-shell)

  (exec-path-from-shell-initialize)
#+end_src

* Appearance

#+begin_src emacs-lisp
  (setq-default tab-width 2)
  (setq-default indent-tabs-mode nil)
  (setq inhibit-startup-screen t)
  (setq inhibit-startup-message t)

  (setq use-short-answers t)

  (delete-selection-mode t)
  (scroll-bar-mode -1)
  (tool-bar-mode -1)
  (menu-bar-mode -1)
  (blink-cursor-mode t)
  (show-paren-mode t)
  (column-number-mode t)
  (tooltip-mode -1)
  (setq ring-bell-function (lambda () (message "*beep*")))

  ;; set proper language (fixes cyrillic letters in ansi-term)
  (setenv "LANG" "fr_FR.UTF-8")

  ;; Fringes
  (fringe-mode '(10 . 10))

  ;; Set default font
  (set-face-attribute 'default nil
                      :family "Source Code Pro"
                      :height 100
                      :weight 'normal
                      :width 'normal)

  ;; Set Frame title with file path
  (setq frame-title-format
        '((:eval (if (buffer-file-name)
                     (abbreviate-file-name (buffer-file-name))
                   "%b"))))

  ;; Vertical splits by default
  (setq split-width-threshold 1)

  ;; Soft morning theme
  (use-package soft-morning-theme
    :defer t
    :init
    (load-theme 'soft-morning t))

  ;; ivy icons
  (use-package all-the-icons-ivy
    :init (add-hook 'after-init-hook 'all-the-icons-ivy-setup))

  (global-visual-line-mode 1)
#+end_src

** Emojis
#+begin_src emacs-lisp
  (use-package emojify
    :hook (after-init . global-emojify-mode)
    :defer t)
  (set-fontset-font t '(#x1f300 . #x1fad0) (font-spec :family "Noto Color Emoji"))
#+end_src

** Focus mode
#+begin_src emacs-lisp
  (use-package focus)
#+end_src

* Various functions

Edit this file

#+begin_src emacs-lisp
  (defun find-config ()
    "Edit config.org"
    (interactive)
    (find-file "~/.emacs.d/config.org"))

  (global-set-key (kbd "C-c I") 'find-config)
#+end_src

Extract value from .netrc

#+begin_src emacs-lisp
  (defun jxx-netrc-get (machine key)
    (netrc-get (netrc-machine (netrc-parse "~/.netrc") machine) key))
#+end_src

#+begin_src emacs-lisp
  (defun switch-to-previous-buffer ()
    (interactive)
    (switch-to-buffer (other-buffer (current-buffer) 1)))

  (defun revert-all-buffers ()
    "Refreshes all open buffers from their respective files."
    (interactive)
    (dolist (buf (buffer-list))
      (with-current-buffer buf
        (when (and (buffer-file-name) (not (buffer-modified-p)))
          (revert-buffer t t t) )))
    (message "Refreshed open files.") )

  ;; File and buffers
  (defun rename-file-and-buffer ()
    "Rename the current buffer and file it is visiting."
    (interactive)
    (let ((filename (buffer-file-name)))
      (if (not (and filename (file-exists-p filename)))
          (message "Buffer is not visiting a file!")
        (let ((new-name (read-file-name "New name: " filename)))
          (cond
           ((vc-backend filename) (vc-rename-file filename new-name))
           (t
            (rename-file filename new-name t)
            (set-visited-file-name new-name t t)))))))

  (global-set-key (kbd "C-c r")  'rename-file-and-buffer)

  (defun delete-file-and-buffer ()
    "Kill the current buffer and deletes the file it is visiting."
    (interactive)
    (let ((filename (buffer-file-name)))
      (when filename
        (if (vc-backend filename)
            (vc-delete-file filename)
          (progn
            (delete-file filename)
            (message "Deleted file %s" filename)
            (kill-buffer))))))

  (define-key isearch-mode-map (kbd "C-d")
              'fc/isearch-yank-symbol)
  (defun fc/isearch-yank-symbol ()
    "Yank the symbol at point into the isearch minibuffer.

  C-w does something similar in isearch, but it only looks for
  the rest of the word. I want to look for the whole string. And
  symbol, not word, as I need this for programming the most."
    (interactive)
    (isearch-yank-string
     (save-excursion
       (when (and (not isearch-forward)
                  isearch-other-end)
         (goto-char isearch-other-end))
       (thing-at-point 'symbol))))

  ;; Create directory if it doesn't exist
  (defun my-create-non-existent-directory ()
    (let ((parent-directory (file-name-directory buffer-file-name)))
      (when (and (not (file-exists-p parent-directory))
                 (y-or-n-p (format "Directory `%s' does not exist! Create it?" parent-directory)))
        (make-directory parent-directory t))))

  (add-to-list 'find-file-not-found-functions #'my-create-non-existent-directory)

  ;; Select line
  (defun textmate-select-line ()
    "If the mark is not active, select the current line.
  Otherwise, expand the current region to select the lines the region touches."
    (interactive)
    (if mark-active ;; expand the selection to select lines
        (let ((top (= (point) (region-beginning)))
              (p1 (region-beginning))
              (p2 (region-end)))
          (goto-char p1)
          (beginning-of-line)
          (push-mark (point))
          (goto-char p2)
          (unless (looking-back "\n")
            (progn
              (end-of-line)
              (if (< (point) (point-max)) (forward-char))))
          (setq mark-active t
                transient-mark-mode t)
          (if top (exchange-point-and-mark)))
      (progn
        (beginning-of-line)
        (push-mark (point))
        (end-of-line)
        (if (< (point) (point-max)) (forward-char))
        (setq mark-active t
              transient-mark-mode t))))

  ;; Shift selection
  (defun textmate-shift-right (&optional arg)
    "Shift the line or region to the ARG places to the right.
  A place is considered `tab-width' character columns."
    (interactive)
    (let ((deactivate-mark nil)
          (beg (or (and mark-active (region-beginning))
                   (line-beginning-position)))
          (end (or (and mark-active (region-end)) (line-end-position))))
      (indent-rigidly beg end (* (or arg 1) tab-width))))

  (defun textmate-shift-left (&optional arg)
    "Shift the line or region to the ARG places to the left."
    (interactive)
    (textmate-shift-right (* -1 (or arg 1))))

  (defun sort-words (reverse beg end)
    "Sort words in region alphabetically, in REVERSE if negative.
    Prefixed with negative \\[universal-argument], sorts in reverse.

    The variable `sort-fold-case' determines whether alphabetic case
    affects the sort order.

    See `sort-regexp-fields'."
    (interactive "*P\nr")
    (sort-regexp-fields reverse "\\w+" "\\&" beg end))

  (defun copy-buffer-path ()
    "Copy the current buffer file path to the clipboard."
    (interactive)
    (let ((path (if (equal major-mode 'dired-mode)
                    default-directory
                  (buffer-file-name))))
      (when path
        (kill-new path)
        (message "Copied buffer file path '%s' to the clipboard." path))))

  ;; Smart move to begining of line
  (use-package crux
    :bind (("C-a" . crux-move-beginning-of-line)))
#+end_src

* Org

#+begin_src emacs-lisp
  (use-package org
    :defer t
    :config
    (setq org-use-sub-superscripts '{}
          org-export-with-sub-superscripts '{}
          org-reverse-note-order t
          org-support-shift-select t))
#+end_src

** Prettier org-mode.

#+begin_src emacs-lisp
  (use-package org-superstar
    :config
    (setq org-superstar-special-todo-items t)
    (setq org-superstar-todo-bullet-alist
          '(("TODO" . ?☐)
            ("DOING" . ?◈)
            ("DONE" . ?☒)
            ("MEETING" . ?⧖)
            ("POSTPONED" . ?→)))

    (add-hook 'org-mode-hook (lambda ()
                               (org-superstar-mode 1))))
#+end_src

** org-todo states

#+begin_src emacs-lisp
  (setq org-todo-keywords
        '((sequence "TODO" "DOING" "MEETING" "|" "DONE" "POSTPONED")))
#+end_src

** capture templates
#+begin_src emacs-lisp
  ;; (setq org-capture-templates
  ;;   `(("m" "Email Workflow")
  ;;     ("mf" "Follow Up" entry (file+olp (org-roam-dailies-find-today) "Follow Up")
  ;;           "* TODO %a")
  ;;     ("mr" "Read Later" entry (file+olp "~/org/Mail.org" "Read Later")
  ;;           "* TODO %a")))
#+end_src

** org-roam
#+begin_src emacs-lisp
  (setq org-directory (concat (getenv "HOME") "/Documents/org-roam/"))
  (setq org-roam-dailies-directory "journal/")

  ;; Improve org mode looks
  (setq org-startup-indented t
        org-pretty-entities t
        org-hide-emphasis-markers t
        org-startup-with-inline-images t
        org-image-actual-width '(300))

  (use-package org-roam
    :after org
    :init
    (setq org-roam-v2-ack t)
    :custom
    (org-roam-directory (file-truename org-directory))
    (org-roam-completion-everywhere t)
    :bind (("C-c n l" . org-roam-buffer-toggle)
           ("C-c n f" . org-roam-node-find)
           ("C-c n i" . org-roam-node-insert)
           :map org-mode-map
           ("C-M-i" . completion-at-point)
           :map org-roam-dailies-map
           ("Y" . org-roam-dailies-capture-yesterday)
           ("T" . org-roam-dailies-capture-tomorrow))
    :bind-keymap
    ("C-c n d" . org-roam-dailies-map)
    :config
    (require 'org-roam-dailies) ;; Ensure the keymap is available
    (org-roam-db-autosync-mode))

  (defun my/org-roam-extract-incomplete-tasks (file)
    "Return top-level TODO/DOING subtrees from FILE as a string."
    (with-temp-buffer
      (insert-file-contents file)
      (org-mode)
      (let (tasks)
        (org-map-entries
         (lambda ()
           (when (and (= (org-current-level) 1)
                      (member (org-get-todo-state) '("TODO" "DOING")))
             (push (buffer-substring-no-properties
                    (point)
                    (save-excursion (org-end-of-subtree t t) (point)))
                   tasks)))
         nil nil)
        (when tasks
          (mapconcat #'identity (nreverse tasks) "")))))

  (defun my/org-roam-daily-prepopulate (&rest _)
    "Pre-populate a new daily file with unfinished tasks from the previous daily.
  Runs as advice after `org-roam-dailies-goto-today' so the header is already written."
    (when-let* ((file (buffer-file-name))
                (daily-dir (file-name-as-directory
                            (expand-file-name org-roam-dailies-directory org-roam-directory)))
                (_ (string-prefix-p daily-dir file))
                (_ (not (save-excursion
                          (goto-char (point-min))
                          (re-search-forward "^\\*" nil t)))))
      (let* ((today (file-name-base file))
             (prev-file (car (last (seq-filter
                                    (lambda (f) (string< (file-name-base f) today))
                                    (sort (directory-files daily-dir t "\\.org$") #'string<))))))
        (when prev-file
          (when-let* ((tasks (my/org-roam-extract-incomplete-tasks prev-file)))
            (goto-char (point-max))
            (unless (bolp) (insert "\n"))
            (insert "\n")
            (insert tasks))))))

  (advice-add 'org-roam-dailies-goto-today :after #'my/org-roam-daily-prepopulate)

  (use-package deft
    :config
    (setq deft-directory org-directory
          deft-recursive t
          deft-strip-summary-regexp ":PROPERTIES:\n\\(.+\n\\)+:END:\n"
          deft-use-filename-as-title t)
    :bind
    ("C-c n s" . deft))
#+end_src

** org-download
#+begin_src emacs-lisp
  ;; Emacs 32 removed url-handler-file-remote-p; keep org-download compatible.
  (unless (fboundp 'url-handler-file-remote-p)
    (defalias 'url-handler-file-remote-p #'file-remote-p))

  (use-package org-download)
#+end_src

* Email

Load mu4e.

#+begin_src emacs-lisp
  (use-package mu4e
    :ensure nil
    :defer t
    :load-path "/usr/share/emacs/site-lisp/mu4e")
#+end_src

Add some notifications.

#+begin_src emacs-lisp
  (use-package alert)
  (use-package mu4e-alert
    :init
    (setq mu4e-alert-set-default-style 'libnotify)
    (add-hook 'after-init-hook #'mu4e-alert-enable-mode-line-display)
    (add-hook 'after-init-hook #'mu4e-alert-enable-notifications))
#+end_src

Make it prettier.

#+begin_src emacs-lisp
  (use-package mu4e-column-faces
    :after mu4e
    :config (mu4e-column-faces-mode))
#+end_src

#+begin_src emacs-lisp
  (require 'smtpmail)
  (require 'mu4e-org)

  ;; Mu4e general settings
  (setq mail-user-agent 'mu4e-user-agent ;; Use mu4e as default Emacs mail agent
        mu4e-maildir "~/Maildir"

        ;; Use mbsync for mail sync
        mu4e-get-mail-command "mbsync -a"
        mu4e-change-filenames-when-moving t
        mu4e-update-interval 120

        ;; Don't save message to Sent Messages, Gmail/IMAP takes care of this
        ;; Override in context switching for other type of mailboxes
        ;; mu4e-sent-messages-behavior 'delete
        message-kill-buffer-on-exit t

        ;; This fixes the error 'mbsync error: UID is x beyond highest assigned UID x'
        mu4e-change-filenames-when-moving t

        ;; Eye candies & attachment handling
        ;; mu4e-use-fancy-chars t
        mu4e-view-show-images t
        ;; mu4e-headers-skip-duplicates t
        mu4e-attachment-dir "~/Downloads"

        ;; Store link to message if in header view, not to header query
        ;; mu4e-org-link-query-in-headers-mode nil

        ;; This helps when using a dark theme (shr)
        ;; shr-color-visible-luminance-min 80

        ;; Citation format
        message-citation-line-format "On %a, %b %d %Y, %N wrote:"
        message-citation-line-function 'message-insert-formatted-citation-line

        ;; Always use 587 for sending emails
        message-send-mail-function 'smtpmail-send-it
        starttls-use-gnutls t
        smtpmail-smtp-service 587

        ;; Use 'helm' to for mailbox selection completion
        mu4e-completing-read-function 'completing-read

        ;; Disale colors in html mails
        shr-use-colors t

        ;; Always display plain-text version
        mu4e-view-prefer-html nil
        mu4e-view-html-plaintext-ratio-heuristic most-positive-fixnum

        ;; Context switch policy
        mu4e-context-policy 'ask
        mu4e-compose-context-policy nil)

  ;; Add option to view html message in a browser
  ;; `aV` in view to activate
  (add-to-list 'mu4e-view-actions
               '("ViewInBrowser" . mu4e-action-view-in-browser) t)

  (add-hook 'mu4e-view-mode-hook
            (lambda () (setq-local browse-url-browser-function #'eww-browse-url)))


  (with-eval-after-load 'mm-decode
    (add-to-list 'mm-discouraged-alternatives "text/html")
    (add-to-list 'mm-discouraged-alternatives "text/richtext"))

  ;; Mu4e contexts

  ;; This will ensure the right 'sent from' address and email sign off etc. be
  ;; picked up when replying to emails.
  (setq mu4e-contexts
        `(
          ,(make-mu4e-context
            :name "typedhole"
            :enter-func (lambda () (mu4e-message "Entering TypedHole account context"))
            :leave-func (lambda () (mu4e-message "Leaving TypedHole account context"))
            ;; We match based on the contact-fields of the message
            :match-func (lambda (msg)
                          (when msg
                            (mu4e-message-contact-field-matches msg :to "julien@typed-hole.org")))
            :vars '((user-mail-address . "julien@typed-hole.org")
                    (user-full-name . "Julien Blanchard")
                    (mu4e-drafts-folder . "/TypedHole/Drafts")
                    (mu4e-sent-folder . "/TypedHole/Sent")
                    (mu4e-trash-folder . "/TypedHole/Trash")
                    (jxx-mu4e-junk-folder . "/TypedHole/.Junk")
                    (mu4e-refile-folder . "/TypedHole/Archive")
                    (smtpmail-smtp-server . "mail.typed-hole.org")
                    (smtpmail-smtp-user . "julien")
                    (smtpmail-starttls-credentials . '(("mail.typed-hole.org" 587 nil nil)))
                    (smtpmail-auth-credentials . '(("mail.typed-hole.org" 587 "julien" nil)))
                    (smtpmail-default-smtp-server . "mail.typed-hole.org")))

          ,(make-mu4e-context
            :name "sideburns"
            :enter-func (lambda () (mu4e-message "Entering Sideburns account context"))
            :leave-func (lambda () (mu4e-message "Leaving Sideburns account context"))
            ;; We match based on the contact-fields of the message
            :match-func (lambda (msg)
                          (when msg
                            (mu4e-message-contact-field-matches msg :to "julien@sideburns.eu")))
            :vars '((user-mail-address . "julien@sideburns.eu")
                    (user-full-name . "Julien Blanchard")
                    (mu4e-drafts-folder . "/Sideburns/Drafts")
                    (mu4e-sent-folder . "/Sideburns/Sent")
                    (mu4e-trash-folder . "/Sideburns/Trash")
                    (jxx-mu4e-junk-folder . "/Sideburns/Junk")
                    (mu4e-refile-folder . "/Sideburns/Archive")
                    (smtpmail-smtp-server . "smtp.fastmail.com")
                    (smtpmail-smtp-user . "julienblanchard@fastmail.fm")
                    (smtpmail-starttls-credentials . '(("smtp.fastmail.com" 465 nil nil)))
                    (smtpmail-auth-credentials . '(("smtp.fastmail.com" 465 "julienblanchard@fastmail.fm" nil)))
                    (smtpmail-default-smtp-server . "smtp.fastmail.com")))

          ,(make-mu4e-context
            :name "gmail"
            :enter-func (lambda () (mu4e-message "Entering Gmail context"))
            :leave-func (lambda () (mu4e-message "Leaving Gmail context"))
            :match-func (lambda (msg)
                          (when msg
                            (mu4e-message-contact-field-matches msg :to "julien.blanchard75@gmail.com")))
            :vars '((user-mail-address . "julien.blanchard75@gmail.com")
                    (user-full-name . "Julien Blanchard")
                    (mu4e-compose-signature . (concat "Liwen Knight-Zhang | +44 (0)7894 222 323\n"))
                    (mu4e-drafts-folder . "/Gmail/[Gmail]/Drafts")
                    (mu4e-sent-folder . "/Gmail/[Gmail]/Sent Mail")
                    (mu4e-trash-folder . "/Gmail/[Gmail]/Trash")
                    (jxx-mu4e-junk-folder . "/Gmail/[Gmail]/Trash")
                    (mu4e-refile-folder . "/Gmail/All Mail")
                    (smtpmail-smtp-server . "smtp.gmail.com")
                    (smtpmail-smtp-user . "julien.blanchard75@gmail.com")
                    (smtpmail-starttls-credentials . '(("smtp.gmail.com" 587 nil nil)))
                    (smtpmail-auth-credentials . '(("smtp.gmail.com" 587 "julien.blanchard75@gmail.com" nil)))))))

  (defun jxx/mu4e-get-junk-folder (&optional msg)
    "Return the junk folder for MSG based on its context."
    (let ((ctx (mu4e-context-determine msg nil)))
      (with-mu4e-context-vars ctx
        (or (and (boundp 'jxx-mu4e-junk-folder) jxx-mu4e-junk-folder)
            (mu4e-get-trash-folder msg)))))

  (defun jxx/mu4e-mark-for-junk ()
    "Mark the current message for moving to the configured junk folder."
    (interactive)
    (mu4e--in-headers-context
     (let* ((msg (mu4e-message-at-point))
            (target (jxx/mu4e-get-junk-folder msg)))
       (unless target
         (mu4e-error "No junk folder configured for this context"))
       (mu4e-mark-set 'move target)
       (when mu4e-headers-advance-after-mark
         (mu4e-headers-next)))))

  (defun jxx/mu4e-get-archive-folder (msg)
    (let ((maildir (mu4e-message-field msg :maildir)))
      (cond
       ((string-prefix-p "/Gmail" maildir)     "/Gmail/All Mail")
       ((string-prefix-p "/TypedHole" maildir) "/TypedHole/Archive")
       ((string-prefix-p "/Sideburns" maildir) "/Sideburns/Archive")
       (t (mu4e-error "No archive folder for maildir: %s" maildir)))))

  (defun jxx/mu4e-archive ()
    "Mark the current message for moving to its account's archive folder."
    (interactive)
    (mu4e--in-headers-context
     (let* ((msg (mu4e-message-at-point))
            (target (jxx/mu4e-get-archive-folder msg)))
       (mu4e-mark-set 'move target)
       (when mu4e-headers-advance-after-mark
         (mu4e-headers-next)))))

  (with-eval-after-load 'mu4e
    (define-key mu4e-headers-mode-map (kbd "J") #'jxx/mu4e-mark-for-junk)
    (define-key mu4e-view-mode-map (kbd "J") #'jxx/mu4e-mark-for-junk)
    (define-key mu4e-headers-mode-map (kbd "A") #'jxx/mu4e-archive)
    (define-key mu4e-view-mode-map (kbd "A") #'jxx/mu4e-archive))

  (add-to-list 'mu4e-bookmarks
               '( :name  "Inbox"
                  :query "m:/Gmail/Inbox OR m:/Sideburns/INBOX OR m:/TypedHole/INBOX"
                  :key   ?i))

  ;; Keep the default unread shortcut (`u`) focused on inboxes only.
  (let ((unread-bookmark
         (seq-find (lambda (bookmark)
                     (string= (plist-get bookmark :name) "Unread messages"))
                   mu4e-bookmarks)))
    (when unread-bookmark
      (setf (plist-get unread-bookmark :query)
            "flag:unread AND NOT flag:trashed AND (maildir:/Gmail/Inbox OR maildir:/Sideburns/INBOX OR maildir:/TypedHole/INBOX)")))

  (set-face-underline 'mu4e-header-highlight-face nil)

  ;; headers
  (setq mu4e-headers-fields '((:human-date . 12)
                              (:from . 22)
                              (:subject)))

  ;; Use imagemagick, if available
  (when (fboundp 'imagemagick-register-types)
    (imagemagick-register-types))

  ;; Emulate shr key bindings
  (add-hook 'mu4e-view-mode-hook
            (lambda()
              ;; try to emulate some of the eww key-bindings
              (local-set-key (kbd "<tab>") 'shr-next-link)
              (local-set-key (kbd "<backtab>") 'shr-previous-link)))

  (setq mu4e-alert-email-notification-types '(count))
#+end_src

Use B to open link in firefox rather than eww

#+begin_src emacs-lisp
  (defun jxx/mu4e-view-go-to-url-gui ()
    "Wrapper for mu4e-view-go-to-url to use gui browser instead of eww"
    (interactive)
    (let ((browse-url-browser-function 'browse-url-default-browser))
      (mu4e-view-go-to-url)))
  ;; bind it
  (define-key mu4e-view-mode-map (kbd "B") 'jxx/mu4e-view-go-to-url-gui)
#+end_src

* Ivy

#+begin_src emacs-lisp
  (use-package smex)
  (use-package counsel)
  (use-package ivy
    :config
    (require 'counsel)
    (ivy-mode 1)
    (setq ivy-use-virtual-buffers t)
    (define-key read-expression-map (kbd "C-r") 'counsel-expression-history))

  (use-package prescient
    :config
    (prescient-persist-mode t))
  (use-package ivy-prescient
    :config
    (ivy-prescient-mode t))

  (use-package ivy-hydra)
#+end_src

* Autocomplete

Corfu completion

#+begin_src emacs-lisp
  (use-package corfu
    :custom
    (corfu-cycle t)                ;; Enable cycling for `corfu-next/previous'
    (corfu-auto t)                 ;; Enable auto completion
    (corfu-auto-delay 0.2)         ;; Delay before auto completion
    (corfu-auto-prefix 2)          ;; Minimum prefix length for auto completion
    (corfu-quit-no-match 'separator) ;; Don't quit if there's no match
    (corfu-preview-current nil)    ;; Disable current candidate preview
    (corfu-preselect 'prompt)      ;; Preselect the prompt
    :init
    (global-corfu-mode)
    :config
    (setq corfu-excluded-modes '(term-mode)))

  (use-package cape
    :init
    (add-to-list 'completion-at-point-functions #'cape-dabbrev)
    (add-to-list 'completion-at-point-functions #'cape-file))

  (use-package corfu-popupinfo
    :ensure nil
    :after corfu
    :hook (corfu-mode . corfu-popupinfo-mode)
    :custom
    (corfu-popupinfo-delay '(0.5 . 0.2)))
#+end_src

* Backup

#+begin_src emacs-lisp
  (setq make-backup-files nil)
  (setq auto-save-default nil)

  ;; Auto refresh buffers
  (global-auto-revert-mode 1)

  ;; Also auto refresh dired, but be quiet about it
  (setq global-auto-revert-non-file-buffers t)
  (setq auto-revert-verbose nil)
#+end_src

* Dired

#+begin_src emacs-lisp
  (use-package dired
    :ensure nil
    :hook (dired-mode . dired-hide-details-mode)
    :config
    ;; Colourful columns.
    (use-package diredfl
      :config
      (diredfl-global-mode 1)))

  (use-package dired-git-info
    :bind (:map dired-mode-map
                (")" . dired-git-info-mode)))
#+end_src

* Editing

#+begin_src emacs-lisp
  (use-package expand-region
    :config
    (global-set-key (kbd "C-=") 'er/expand-region))

  (use-package wrap-region
    :config
    (wrap-region-global-mode))

  ;; Hit C-c <tab> to auto-indent the entire buffer you're in.
  (defun indent-buffer ()
    (interactive)
    (indent-region (point-min) (point-max)))
  (global-set-key (kbd "C-c <tab>") 'indent-buffer)

  (defun duplicate-line (arg)
    "Duplicate current line, leaving point in lower line."
    (interactive "*p")

    ;; save the point for undo
    (setq buffer-undo-list (cons (point) buffer-undo-list))

    ;; local variables for start and end of line
    (let ((bol (save-excursion (beginning-of-line) (point)))
          eol)
      (save-excursion

        ;; don't use forward-line for this, because you would have
        ;; to check whether you are at the end of the buffer
        (end-of-line)
        (setq eol (point))

        ;; store the line and disable the recording of undo information
        (let ((line (buffer-substring bol eol))
              (buffer-undo-list t)
              (count arg))
          ;; insert the line arg times
          (while (> count 0)
            (newline)         ;; because there is no newline in 'line'
            (insert line)
            (setq count (1- count)))
          )

        ;; create the undo information
        (setq buffer-undo-list (cons (cons eol (point)) buffer-undo-list)))
      ) ; end-of-let

    ;; put the point in the lowest line and return
    (next-line arg))

  ;; Behave like vi's o command
  (defun open-line-below ()
    (interactive)
    (end-of-line)
    (newline)
    (indent-for-tab-command))

  (defun open-line-above ()
    (interactive)
    (beginning-of-line)
    (newline)
    (forward-line -1)
    (indent-for-tab-command))

  ;; custom goto-line
  ;; turn line numbers off by default
  (display-line-numbers-mode -1)

  (defun goto-line-with-feedback (&optional line)
    "Show line numbers temporarily, while prompting for the LINE number input."
    (interactive "P")
    (if line
        (goto-line line)
      (unwind-protect
          (progn
            (display-line-numbers-mode 1)
            (goto-line (read-number "Goto line: ")))
        (display-line-numbers-mode -1))))

  ;; Toggle between split windows and a single window
  (defun toggle-windows-split()
    "Switch back and forth between one window and whatever split of windows we might have in the frame. The idea is to maximize the current buffer, while being able to go back to the previous split of windows in the frame simply by calling this command again."
    (interactive)
    (if (not (window-minibuffer-p (selected-window)))
        (progn
          (if (< 1 (count-windows))
              (progn
                (window-configuration-to-register ?u)
                (delete-other-windows))
            (jump-to-register ?u))))
    (my-iswitchb-close))

  (defun zap-up-to-char (arg char)
    "Kill up to, but not including ARGth occurrence of CHAR.
  Case is ignored if `case-fold-search' is non-nil in the current buffer.
  Goes backward if ARG is negative; error if CHAR not found.
  Ignores CHAR at point."
    (interactive "p\ncZap up to char: ")
    (let ((direction (if (>= arg 0) 1 -1)))
      (kill-region (point)
                   (progn
                     (forward-char direction)
                     (unwind-protect
                         (search-forward (char-to-string char) nil nil arg)
                       (backward-char direction))
                     (point)))))

  (defun move-line-down ()
    (interactive)
    (let ((col (current-column)))
      (save-excursion
        (forward-line)
        (transpose-lines 1))
      (forward-line)
      (move-to-column col)))

  (defun move-line-up ()
    (interactive)
    (let ((col (current-column)))
      (save-excursion
        (forward-line)
        (transpose-lines -1))
      (move-to-column col)))

  (defun rotate-windows ()
    "Rotate your windows"
    (interactive)
    (cond ((not (> (count-windows)1))
           (message "You can't rotate a single window!"))
          (t
           (let ((i 1)
                 (numWindows (count-windows)))
             (while (< i numWindows)
               (let* ((w1 (elt (window-list) i))
                      (w2 (elt (window-list) (+ (% i numWindows) 1)))
                      (b1 (window-buffer w1))
                      (b2 (window-buffer w2))
                      (s1 (window-start w1))
                      (s2 (window-start w2)))
                 (set-window-buffer w1 b2)
                 (set-window-buffer w2 b1)
                 (set-window-start w1 s2)
                 (set-window-start w2 s1)
                 (setq i (1+ i))))))))
#+end_src

* Flycheck

#+begin_src emacs-lisp
  (use-package flycheck
    :config
    (add-hook 'after-init-hook #'global-flycheck-mode))
#+end_src

* Hooks

#+begin_src emacs-lisp
  ;; Line numbers
  (add-hook 'eshell-mode-hook
            '(lambda () (display-line-numbers-mode -1)))

  (add-hook 'compilation-mode-hook
            '(lambda () (display-line-numbers-mode -1)))

  (add-hook 'shell-mode-hook
            '(lambda () (display-line-numbers-mode -1)))

  (add-hook 'magit-mode-hook
            '(lambda () (display-line-numbers-mode -1)))

  ;; SLIME hooks
  (add-hook 'sldb-mode-hook
            #'(lambda ()
                (autopair-mode -1)))

  ;; Delete trailing whitespaces
  (add-hook 'before-save-hook 'delete-trailing-whitespace)

  ;; Rainbow mode hooks
  (add-hook 'css-ts-mode-hook 'rainbow-mode)
  (add-hook 'scss-mode-hook 'rainbow-mode)
  (add-hook 'sass-mode-hook 'rainbow-mode)

  ;; C-d to exit shell
  (defun comint-delchar-or-eof-or-kill-buffer (arg)
    (interactive "p")
    (if (null (get-buffer-process (current-buffer)))
        (kill-buffer)
      (comint-delchar-or-maybe-eof arg)))

  (add-hook 'shell-mode-hook
            (lambda ()
              (define-key shell-mode-map
                          (kbd "C-d") 'comint-delchar-or-eof-or-kill-buffer)))

  (add-hook 'markdown-mode-hook
            (lambda ()
              (local-set-key (kbd "C-c o p") 'jxx-markdown-preview)))

  ;; tabs size 8 for Go
  (add-hook 'go-ts-mode-hook (lambda () (setq tab-width 8)))
#+end_src

* GNUS

#+begin_src emacs-lisp
  (use-package gnus
    :defer t
    :config
    (setq user-mail-address "julien@typed-hole.org"
          user-full-name    "Julien")

    (setq gnus-select-method '(nntp "news.tilde.club" (nntp-port-number 119)))

    (add-to-list 'gnus-secondary-select-methods '(nntp "news.eternal-september.org" (nntp-port-number 119)))

    (setq send-mail-function       'smtpmail-send-it
          message-send-mail-function 'smtpmail-send-it
          smtpmail-smtp-server     "mail.typed-hole.org")

    (setq gnus-permanently-visible-groups ".")
    (setq gnus-large-newsgroup 500)
    (setq gnus-thread-sort-functions '(gnus-thread-sort-by-most-recent-date))
    (setq gnus-article-sort-functions '((not gnus-article-sort-by-date)))
    (add-hook 'gnus-started-hook
              (lambda ()
                (with-current-buffer gnus-group-buffer
                  (gnus-group-list-all-groups))))

    ;; local article cache via Gnus Agent
    (setq gnus-agent t)
    (setq gnus-agent-auto-agentize-methods '(nntp))

    ;; auto-save: always restore without prompting
    (setq gnus-always-read-dribble-file t)

    ;; mu4e-style date: time for today, date for older
    (setq gnus-user-date-format-alist
          '(((gnus-seconds-today) . "%H:%M")
            (t                    . "%d/%m/%Y")))

    ;; 3-column layout: date | from | subject (full subject always shown)
    (setq gnus-summary-line-format "%-10,10&user-date;  %-25,25f  %B%S\n")

    ;; minimal thread tree
    (setq gnus-sum-thread-tree-indent          "  ")
    (setq gnus-sum-thread-tree-root            "")
    (setq gnus-sum-thread-tree-false-root      "")
    (setq gnus-sum-thread-tree-single-indent   "")
    (setq gnus-sum-thread-tree-vertical        "│ ")
    (setq gnus-sum-thread-tree-leaf-with-other "├─ ")
    (setq gnus-sum-thread-tree-single-leaf     "└─ ")

    ;; column headers + keybindings
    (add-hook 'gnus-summary-mode-hook
              (lambda ()
                (hl-line-mode 1)
                (setq header-line-format
                      (format " %-10s  %-25s  %s" "Date" "From" "Subject"))
                (local-set-key (kbd "RET")
                               (lambda ()
                                 (interactive)
                                 (gnus-summary-select-article)
                                 (let ((w (get-buffer-window gnus-article-buffer)))
                                   (when w (select-window w)))))))

    ;; selected row: theme hl-line color, no underline
    (set-face-attribute 'gnus-summary-selected nil
                        :background "#d3d2d1"
                        :underline nil))
#+end_src

* Libs

#+begin_src emacs-lisp
  (defun jxx/exec (command)
    "Run a shell command and return its output as a string, whitespace trimmed."
    (s-trim (shell-command-to-string command)))

  (defun jxx/is-exec (command)
    "Returns true if `command' is an executable on the system search path."
    (f-executable? (s-trim (shell-command-to-string (s-concat "which " command)))))

  (defun jxx/resolve-exec (command)
    "If `command' is an executable on the system search path, return its absolute path.
  Otherwise, return nil."
    (-let [path (s-trim (shell-command-to-string (s-concat "which " command)))]
      (when (f-executable? path) path)))

  (defun jxx/exec-if-exec (command args)
    "If `command' satisfies `jxx/is-exec', run it with `args' and return its
  output as per `jxx/exec'. Otherwise, return nil."
    (when (jxx/is-exec command) (jxx/exec (s-concat command " " args))))

  (defun jxx/getent (user)
    "Get the /etc/passwd entry for the user `user' as a list of strings,
  or nil if there is no such user. Empty fields will be represented as nil,
  as opposed to empty strings."
    (-let [ent (jxx/exec (s-concat "getent passwd " user))]
      (when (not (s-blank? ent))
        (-map (lambda (i) (if (s-blank? i) nil i))
              (s-split ":" ent)))))

  (defun jxx/user-full-name ()
    "Guess the user's full name. Returns nil if no likely name could be found."
    (or (jxx/exec-if-exec "git" "config --get user.name")
        (elt (jxx/getent (getenv "USER")) 4)))

  (defun jxx/user-email ()
    "Guess the user's email address. Returns nil if none could be found."
    (or (jxx/exec-if-exec "git" "config --get user.email")
        (getenv "EMAIL")))
#+end_src

* Modes

#+begin_src emacs-lisp
  (use-package go-ts-mode
    :ensure nil
    :mode "\\.go\\'"
    :config
    (setq go-ts-mode-indent-offset 4)
    (add-hook 'go-ts-mode-hook (lambda () (setq tab-width 4)))
    ;; Add GOPATH/bin to exec-path for gopls
    (let ((gopath (or (getenv "GOPATH") (concat (getenv "HOME") "/go"))))
      (add-to-list 'exec-path (concat gopath "/bin"))))

  ;; Disable Go flycheck checkers — eglot/gopls provides diagnostics already
  (with-eval-after-load 'flycheck
    (add-hook 'go-ts-mode-hook
              (lambda ()
                (flycheck-disable-checker 'go-vet)
                (flycheck-disable-checker 'go-build)
                (flycheck-disable-checker 'go-errcheck)
                (flycheck-disable-checker 'go-golint))))

  ;; We never want to edit Rubinius bytecode or MacRuby binaries
  (add-to-list 'completion-ignored-extensions ".rbc")
  (add-to-list 'completion-ignored-extensions ".rbo")

  ;; Treat some files as shell scripts
  (add-to-list 'auto-mode-alist '(".gitconfig$" . conf-mode))
  (add-to-list 'auto-mode-alist '(".zshrc$" . sh-mode))
  (add-to-list 'auto-mode-alist '(".zshenv$" . sh-mode))

  ;; Markdown
  (add-to-list 'auto-mode-alist '("\\.markdown" . markdown-mode))
  (add-to-list 'auto-mode-alist '("\\.markdn"   . markdown-mode))
  (add-to-list 'auto-mode-alist '("\\.mdown"    . markdown-mode))
  (add-to-list 'auto-mode-alist '("\\.md"       . markdown-mode))

  ;; Javascript with tree-sitter
  (use-package js-ts-mode
    :ensure nil
    :mode "\\.js\\'"
    :config
    (setq js-indent-level 2))

  ;; JSON with tree-sitter
  (use-package json-ts-mode
    :ensure nil
    :mode "\\.json\\'")

  ;; RESTClient
  (add-to-list 'auto-mode-alist '("\\.http" . restclient-mode))

  ;; CSS with tree-sitter
  (use-package css-ts-mode
    :ensure nil
    :mode "\\.css\\'")

  ;; Bash scripts with tree-sitter
  (use-package bash-ts-mode
    :ensure nil
    :mode (("\\.sh\\'" . bash-ts-mode)
           ("\\.bash\\'" . bash-ts-mode)))

  ;; Python with tree-sitter
  (use-package python-ts-mode
    :ensure nil
    :mode "\\.py\\'")

  ;; C/C++ with tree-sitter
  (use-package c-ts-mode
    :ensure nil
    :mode "\\.c\\'")

  (use-package c++-ts-mode
    :ensure nil
    :mode (("\\.cpp\\'" . c++-ts-mode)
           ("\\.hpp\\'" . c++-ts-mode)
           ("\\.cc\\'" . c++-ts-mode)
           ("\\.hh\\'" . c++-ts-mode)))

  ;; TOML with tree-sitter
  (use-package toml-ts-mode
    :ensure nil
    :mode "\\.toml\\'")

  ;; Svelte with tree-sitter
  (define-derived-mode svelte-ts-mode html-mode "Svelte"
    "Major mode for editing Svelte files with tree-sitter."
    (when (treesit-ready-p 'svelte)
      (treesit-parser-create 'svelte)))

  (add-to-list 'auto-mode-alist '("\\.svelte\\'" . svelte-ts-mode))

  ;; Vue with tree-sitter
  (define-derived-mode vue-ts-mode html-mode "Vue"
    "Major mode for editing Vue files with tree-sitter."
    (when (treesit-ready-p 'vue)
      (treesit-parser-create 'vue)))

  (add-to-list 'auto-mode-alist '("\\.vue\\'" . vue-ts-mode))

  ;; Elixir (keeping non-tree-sitter for now as it needs external package)
  (add-to-list 'auto-mode-alist '("\\.exs" . elixir-mode))

  ;; CoffeeScript (no tree-sitter available)
  (add-to-list 'auto-mode-alist '("\\.coffee" . coffee-mode))

  ;; .NET (no tree-sitter available)
  (add-to-list 'auto-mode-alist '("\\.fsproj" . fsharp-mode))
#+end_src

* Magit

#+begin_src emacs-lisp
  (use-package magit
    :defer t
    :config
    (define-key magit-status-mode-map (kbd "C-x C-k") 'magit-kill-file-on-line)
    (define-key magit-status-mode-map (kbd "q") 'magit-quit-session)
    (define-key magit-status-mode-map (kbd "W") 'magit-toggle-whitespace)
    (setq magit-push-always-verify nil))

  (use-package forge
    :after magit
    :config
    (setq forge-status-buffer-default-topic-filters
          (forge--topics-spec :type 'topic :active t :state 'open :order 'newest :limit 40)))

  (defun magit-kill-file-on-line ()
    "Show file on current magit line and prompt for deletion."
    (interactive)
    (magit-visit-item)
    (delete-current-buffer-file)
    (magit-refresh))

  ;; full screen magit-status
  (advice-add 'magit-status :around
              (lambda (orig-fun &rest args)
                (interactive)
                (window-configuration-to-register :magit-fullscreen)
                (apply orig-fun args)
                (delete-other-windows)))

  (defun magit-quit-session ()
    "Restore the previous window configuration and kill the magit buffer."
    (interactive)
    (kill-buffer)
    (jump-to-register :magit-fullscreen))

  ;; ignore whitespace
  (defun magit-toggle-whitespace ()
    (interactive)
    (if (member "-w" magit-diff-options)
        (magit-dont-ignore-whitespace)
      (magit-ignore-whitespace)))

  (defun magit-ignore-whitespace ()
    (interactive)
    (add-to-list 'magit-diff-options "-w")
    (magit-refresh))

  (defun magit-dont-ignore-whitespace ()
    (interactive)
    (setq magit-diff-options (remove "-w" magit-diff-options))
    (magit-refresh))

  ;; close popup when commiting
  (advice-add 'git-commit-commit :after (lambda (&rest _) (delete-window)))
#+end_src

* Mappings

#+begin_src emacs-lisp
  (global-set-key (kbd "C-c s")        'shell)
  (global-set-key (kbd "C-c c")        'switch-to-previous-buffer)
  (global-set-key (kbd "M-T")          'textmate-goto-symbol)
  (global-set-key (kbd "C-c d")        'duplicate-line)
  (global-set-key (kbd "M-]")          'textmate-shift-right)
  (global-set-key (kbd "M-[")          'textmate-shift-left)
  (global-set-key (kbd "M-/")          'comment-or-uncomment-region)
  (global-set-key (kbd "C-x à")        'delete-window)
  (global-set-key (kbd "C-x &")        'delete-other-windows)
  (global-set-key (kbd "C-x é")        'split-window-below)
  (global-set-key (kbd "C-x \"")       'split-window-right)
  (global-set-key (kbd "M-L")          'textmate-select-line)
  (global-set-key (kbd "C-c g")        'magit-status)
  (global-set-key (kbd "M-l")          'goto-line-with-feedback)
  (global-set-key (kbd "M-z")          'zap-up-to-char)
  (global-set-key (kbd "C-x -")        'toggle-windows-split)
  (global-set-key (kbd "<f12>")        'multi-term-dedicated-toggle)
  (global-set-key (kbd "C-c ,,")       'ruby-open-spec-other-buffer)
  (global-set-key (kbd "<C-S-down>")   'move-line-down)
  (global-set-key (kbd "<C-S-up>")     'move-line-up)
  (global-set-key (kbd "<C-return>")   'open-line-below)
  (global-set-key (kbd "<C-S-return>") 'open-line-above)
  (global-set-key (kbd "M-.")          'xref-find-definitions)
  (global-set-key (kbd "M-p")          'previous-multiframe-window)
  (global-set-key (kbd "M-n")          'other-window)
  (global-set-key (kbd "C-ù")          'mark-all-words-like-this)
  (global-set-key "\C-s"               'swiper)
  (global-set-key (kbd "C-c C-r")      'ivy-resume)
  (global-set-key (kbd "<f6>")         'ivy-resume)
  (global-set-key (kbd "M-x")          'counsel-M-x)
  (global-set-key (kbd "M-t")          'project-find-file)
  (global-set-key (kbd "C-c p p")      'project-switch-project)
  (global-set-key (kbd "C-x C-f")      'counsel-find-file)
  (global-set-key (kbd "C-x b")        'ivy-switch-buffer)
  (global-set-key (kbd "C-c j")        'counsel-git-grep)
  (global-set-key (kbd "C-c k")        'counsel-ripgrep)
  (global-set-key (kbd "M-y")          'counsel-yank-pop)
  (global-set-key (kbd "M-SPC")        'avy-goto-char)

  (define-key global-map (kbd "RET") 'newline-and-indent)

  (define-key swiper-map (kbd "C-s")
              (lambda () (interactive) (insert (format "\\<%s\\>" (with-ivy-window (thing-at-point 'symbol))))))
  (define-key swiper-map (kbd "M-s")
              (lambda () (interactive) (insert (format "\\<%s\\>" (with-ivy-window (thing-at-point 'word))))))
#+end_src

* Modeline

#+begin_src emacs-lisp
  (use-package diminish)

  (defface jxx/ml-filename  '((t :background "#e2e2e5" :foreground "#4c4c4c" :weight bold)) "")
  (defface jxx/ml-modified  '((t :background "#e2e2e5" :foreground "#d08689" :weight bold)) "")
  (defface jxx/ml-mode      '((t :background "#e2e2e5" :foreground "#4c4c4c")) "")
  (defface jxx/ml-diag-err  '((t :background "#d08689" :foreground "#ffffff" :weight bold)) "")
  (defface jxx/ml-diag-warn '((t :background "#c9a227" :foreground "#ffffff")) "")
  (defface jxx/ml-diag-ok   '((t :background "#8aa234" :foreground "#ffffff")) "")

  ;; ── diagnostics ivy list ────────────────────────────────────────────────

  (defun jxx/ml-show-diagnostics ()
    (interactive)
    (let ((buf (current-buffer)) candidates)
      (cond
       ((and (bound-and-true-p flycheck-mode) flycheck-current-errors)
        (setq candidates
              (mapcar (lambda (e)
                        (propertize (format "%s %4d:%-3d %s"
                                           (pcase (flycheck-error-level e) ('error "!") ('warning "~") (_ "·"))
                                           (flycheck-error-line e)
                                           (or (flycheck-error-column e) 0)
                                           (flycheck-error-message e))
                                    'jxx-e e))
                      flycheck-current-errors)))
       ((and (bound-and-true-p flymake-mode) (not (bound-and-true-p flycheck-mode))
             (fboundp 'flymake-diagnostics))
        (setq candidates
              (mapcar (lambda (d)
                        (let ((sev (flymake--lookup-type-property (flymake-diagnostic-type d)
                                                                  'severity (warning-numeric-level :error))))
                          (propertize (format "%s %4d  %s"
                                             (cond ((> sev (warning-numeric-level :warning)) "!")
                                                   ((> sev (warning-numeric-level :debug))   "~")
                                                   (t "·"))
                                             (line-number-at-pos (flymake-diagnostic-beg d))
                                             (flymake-diagnostic-text d))
                                      'jxx-d d)))
                      (flymake-diagnostics)))))
      (if (null candidates)
          (message "No diagnostics")
        (ivy-read "Diagnostics: " candidates
                  :action (lambda (c)
                            (with-current-buffer buf
                              (if-let* ((e (get-text-property 0 'jxx-e c)))
                                  (progn (goto-char (point-min))
                                         (forward-line (1- (flycheck-error-line e)))
                                         (move-to-column (max 0 (1- (or (flycheck-error-column e) 1)))))
                                (when-let* ((d (get-text-property 0 'jxx-d c)))
                                  (goto-char (flymake-diagnostic-beg d))))))
                  :caller 'jxx/ml-show-diagnostics))))

  (defvar jxx/ml-diag-map
    (let ((m (make-sparse-keymap)))
      (define-key m [mode-line mouse-1] #'jxx/ml-show-diagnostics) m))

  ;; ── diagnostics segment ─────────────────────────────────────────────────

  (defun jxx/ml--diag (face str)
    (propertize str 'face face 'local-map jxx/ml-diag-map 'mouse-face 'mode-line-highlight))

  (defun jxx/ml--flycheck ()
    (when (bound-and-true-p flycheck-mode)
      (pcase flycheck-last-status-change
        ('running  (jxx/ml--diag 'jxx/ml-mode " … "))
        ('finished
         (let ((e 0) (w 0) (i 0))
           (mapc (lambda (x)
                   (pcase (flycheck-error-level-compilation-level (car x))
                     (2 (cl-incf e (cdr x))) (1 (cl-incf w (cdr x))) (0 (cl-incf i (cdr x)))))
                 (flycheck-count-errors flycheck-current-errors))
           (cond ((> e 0) (jxx/ml--diag 'jxx/ml-diag-err  " ! "))
                 ((> w 0) (jxx/ml--diag 'jxx/ml-diag-warn " ~ "))
                 ((> i 0) (jxx/ml--diag 'jxx/ml-diag-warn " · "))
                 (t       (jxx/ml--diag 'jxx/ml-diag-ok   " ✓ ")))))
        (_ nil))))

  (defun jxx/ml--flymake ()
    (when (and (bound-and-true-p flymake-mode) (not (bound-and-true-p flycheck-mode))
               (fboundp 'flymake-diagnostics))
      (let ((wl (warning-numeric-level :warning)) (nl (warning-numeric-level :debug))
            (e 0) (w 0))
        (dolist (d (flymake-diagnostics))
          (let ((s (flymake--lookup-type-property (flymake-diagnostic-type d)
                                                  'severity (warning-numeric-level :error))))
            (cond ((> s wl) (cl-incf e)) ((> s nl) (cl-incf w)))))
        (jxx/ml--diag (cond ((> e 0) 'jxx/ml-diag-err) ((> w 0) 'jxx/ml-diag-warn) (t 'jxx/ml-diag-ok))
                      (cond ((> e 0) " ! ") ((> w 0) " ~ ") (t " ✓ "))))))

  ;; ── path ────────────────────────────────────────────────────────────────

  (defun jxx/ml-shrink-path (file)
    (if (not file) (buffer-name)
      (let* ((home  (expand-file-name "~"))
             (root  (when (fboundp 'project-current)
                      (when-let* ((p (project-current nil (file-name-directory file))))
                        (expand-file-name (project-root p)))))
             (fname (file-name-nondirectory file))
             (shrink (lambda (ps) (mapconcat (lambda (d) (substring d 0 1)) ps "/"))))
        (if root
            (let* ((parts (seq-filter #'identity
                                      (split-string (or (file-name-directory (file-relative-name file root)) "") "/" t)))
                   (proj  (file-name-nondirectory (directory-file-name root))))
              (if parts (format "%s/%s/%s" proj (funcall shrink parts) fname)
                (format "%s/%s" proj fname)))
          (let* ((abbrev (if (string-prefix-p home file) (concat "~" (substring file (length home))) file))
                 (parts  (seq-filter #'identity (split-string (or (file-name-directory abbrev) "") "/" t)))
                 (last   (car (last parts))))
            (cond ((> (length parts) 1) (format "%s/%s/%s" (funcall shrink (butlast parts)) last fname))
                  (last (format "%s/%s" last fname))
                  (t fname)))))))

  ;; ── path + icon cache ───────────────────────────────────────────────────

  (defvar-local jxx/ml--path nil)
  (defvar-local jxx/ml--icon :unset)

  (defun jxx/ml--reset-path-cache ()
    (setq jxx/ml--path nil jxx/ml--icon :unset))

  (add-hook 'find-file-hook  #'jxx/ml--reset-path-cache)
  (add-hook 'after-save-hook #'jxx/ml--reset-path-cache)

  (defun jxx/ml--compute-icon ()
    (let ((ic (condition-case nil
                  (if (buffer-file-name)
                      (and (fboundp 'all-the-icons-icon-for-file) (all-the-icons-icon-for-file (buffer-file-name)))
                    (and (fboundp 'all-the-icons-icon-for-mode) (all-the-icons-icon-for-mode major-mode)))
                (error nil))))
      (when (and ic (not (string-empty-p ic)))
        (let* ((p (get-text-property 0 'face ic)) (s (copy-sequence ic)))
          (put-text-property 0 (length s) 'face
                             `(:background ,(face-background 'jxx/ml-mode nil t)
                               :foreground ,(or (and (listp p) (plist-get p :foreground)) (face-foreground 'default nil t))
                               :weight normal
                               ,@(when (and (listp p) (plist-get p :family)) `(:family ,(plist-get p :family)))
                               ,@(when (and (listp p) (plist-get p :height)) `(:height ,(plist-get p :height))))
                             s) s))))

  ;; ── moody setup ─────────────────────────────────────────────────────────

  (use-package moody
    :demand t
    :config
    (setq x-underline-at-descent-line t)
    ;; Remove box, wire overline/underline so moody can draw raised tabs.
    ;; Done after theme load so face colours are resolved.
    (set-face-attribute 'mode-line          nil :background "#e2e2e5" :box nil :overline nil :underline nil)
    (set-face-attribute 'mode-line-inactive nil :background "#e2e2e5" :box nil :overline nil :underline nil))

  ;; ── format ──────────────────────────────────────────────────────────────

  (setq-default mode-line-format
                '("%e" (:eval
                  (condition-case err
                    (let* ((modified (and (buffer-file-name) (buffer-modified-p)))
                           (_   (when (eq jxx/ml--icon :unset) (setq jxx/ml--icon (jxx/ml--compute-icon))))
                           (_   (unless jxx/ml--path (setq jxx/ml--path (jxx/ml-shrink-path (buffer-file-name)))))
                           (fly (or (jxx/ml--flycheck) (jxx/ml--flymake) "")))
                      ;; moody-tab returns a list; use list here so mode-line engine
                      ;; recurses into it rather than concat treating it as chars
                      (list
                       fly
                       (propertize " " 'display '(space :width 4) 'face 'jxx/ml-mode)
                       (moody-tab
                        (concat
                         (when jxx/ml--icon
                           (concat (propertize " " 'face 'jxx/ml-mode) jxx/ml--icon (propertize " " 'face 'jxx/ml-mode)))
                         (propertize (format " %s " jxx/ml--path)
                                     'face (if modified 'jxx/ml-modified 'jxx/ml-filename)
                                     'help-echo (or (buffer-file-name) (buffer-name))))
                        nil 'up)
                       (propertize (format-mode-line " %l:%c ") 'face '(:inherit jxx/ml-mode :weight normal))))
                  (error (format " [ml-err: %s] " (error-message-string err)))))))
#+end_src

* Navigation

#+begin_src emacs-lisp
  (use-package xref
    :config
    ;; Show definitions in a completing-read interface
    (setq xref-show-definitions-function #'xref-show-definitions-completing-read))

  ;; Dumb-jump as fallback when LSP is not available
  (use-package dumb-jump
    :config
    (require 'xref)
    ;; Add dumb-jump at the end of xref backends (LSP will be checked first)
    (add-hook 'xref-backend-functions #'dumb-jump-xref-activate 90))

  ;; In Ruby buffers with eglot active, xref stops at eglot's backend (depth 0)
  ;; and never reaches dumb-jump (depth 90).  Wire up a combined backend that
  ;; tries eglot first and falls back to dumb-jump when it returns nothing
  (defun jxx/ruby-xref-backend ()
    (when (eglot-current-server) 'jxx/ruby-xref))

  (cl-defmethod xref-backend-identifier-at-point ((_b (eql jxx/ruby-xref)))
    ;; Eglot's own implementation returns "LSP identifier at point" as a sentinel
    ;; for non-prompting commands (M-.) and uses buffer position for the actual
    ;; LSP request.  Return the real symbol text so dumb-jump can search for it.
    (or (thing-at-point 'symbol t) "LSP identifier at point"))

  (cl-defmethod xref-backend-identifier-completion-table ((_b (eql jxx/ruby-xref)))
    (xref-backend-identifier-completion-table 'eglot))

  (cl-defmethod xref-backend-definitions ((_b (eql jxx/ruby-xref)) identifier)
    (or (xref-backend-definitions 'eglot identifier)
        (xref-backend-definitions (dumb-jump-xref-activate) identifier)))

  (cl-defmethod xref-backend-references ((_b (eql jxx/ruby-xref)) identifier)
    (xref-backend-references 'eglot identifier))

  (cl-defmethod xref-backend-apropos ((_b (eql jxx/ruby-xref)) pattern)
    (xref-backend-apropos 'eglot pattern))

  (add-hook 'ruby-ts-mode-hook
            (lambda ()
              ;; Add before eglot's own hook (depth -10 < eglot's 0) so ours wins
              (add-hook 'xref-backend-functions #'jxx/ruby-xref-backend -10 t)))
#+end_src

** backward-forward, use C-Left or C-Right

#+begin_src emacs-lisp
  (use-package backward-forward
    :config
    (backward-forward-mode t))
#+end_src

** project.el, don't prompt and use file by default

#+begin_src emacs-lisp
  (setq project-switch-commands 'project-find-file)
#+end_src

* Other Packages

#+begin_src emacs-lisp
  (use-package multiple-cursors
    :bind (("C-S-c C-S-c" . mc/edit-lines)
           ("C->"         . mc/mark-next-like-this)
           ("C-<"         . mc/mark-previous-like-this)
           ("C-c C-<"     . mc/mark-all-like-this)))
#+end_src

#+begin_src emacs-lisp
  (use-package pulsar
    :config
    (pulsar-global-mode 1))
#+end_src

#+begin_src emacs-lisp
  (use-package smartparens
    :config
    (require 'smartparens-config)
    (require 'smartparens-ruby)
    (smartparens-global-mode t)
    (show-smartparens-global-mode t)
    (sp-local-pair 'minibuffer-inactive-mode "'" nil :actions nil)

      ;;; markdown-mode
    (sp-with-modes '(markdown-mode gfm-mode rst-mode)
      (sp-local-pair "*" "*" :bind "C-*")
      (sp-local-tag "2" "**" "**")
      (sp-local-tag "s" "```scheme" "```")
      (sp-local-tag "<"  "<_>" "</_>" :transform 'sp-match-sgml-tags))

      ;;; tex-mode latex-mode
    (sp-with-modes '(tex-mode plain-tex-mode latex-mode)
      (sp-local-tag "i" "\"<" "\">"))

      ;;; html-mode
    (sp-with-modes '(html-mode sgml-mode web-mode)
      (sp-local-pair "<" ">"))

      ;;; lisp modes
    (sp-with-modes sp--lisp-modes
      ;; disable ', it's the quote character!
      (sp-local-pair "'" nil :actions nil)
      ;; also only use the pseudo-quote inside strings where it serve as
      ;; hyperlink.
      (sp-local-pair "`" "'" :when '(sp-in-string-p sp-in-comment-p))
      (sp-local-pair "`" nil
                     :skip-match (lambda (ms mb me)
                                   (cond
                                    ((equal ms "'")
                                     (or (sp--org-skip-markup ms mb me)
                                         (not (sp-point-in-string-or-comment))))
                                    (t (not (sp-point-in-string-or-comment)))))))

    (sp-with-modes '(rhtml-mode)
      (sp-local-pair "<" ">")
      (sp-local-pair "<%" "%>")))

  ;; (use-package yasnippet
  ;;  :config
  ;;  (setq yas-snippets-dir
  ;;        '("~/.emacs.d/snippets"))
  ;;  (yas-global-mode 1))

  (use-package gist
    :config
    (require 'eieio)
    (setq gist-use-curl t)
    (setq gist-view-gist t))

  (use-package ace-jump-mode
    :bind ("C-c C-SPC" . ace-jump-mode))

  (use-package multi-term
    :config
    (set-terminal-coding-system 'utf-8-unix)
    (setq multi-term-dedicated-select-after-open-p t)
    (setq multi-term-program "/bin/zsh")
    (setq multi-term-buffer-name "Terminal")

    (defun it-multi-term-dedicated-toggle ()
      "jump back to previous location after toggling ded term off"
      (interactive)
      (if (multi-term-dedicated-exist-p)
          (progn
            (multi-term-dedicated-toggle)
            (switch-to-buffer-other-window old-buf))
        (progn
          (setq old-buf (current-buffer))
          (multi-term-dedicated-toggle))
        )
      ))

  (use-package golden-ratio
    :config
    (golden-ratio-mode))

  (use-package markdown-mode
    :config
    (autoload 'markdown-mode "markdown-mode.el"
      "Major mode for editing Markdown files" t))

  (use-package git-gutter
    :config
    (global-git-gutter-mode t)
    (setq git-gutter:modified-sign "<> ")
    (setq git-gutter:added-sign "++ ")
    (setq git-gutter:deleted-sign "-- "))


  (use-package anzu
    :config
    (global-anzu-mode +1))

  (use-package unkillable-scratch
    :config
    (unkillable-scratch 1))

  (use-package popwin
    :defer t
    :config
    (popwin-mode 1))

  (use-package which-key
    :config
    (add-hook 'after-init-hook 'which-key-mode))

  (use-package undo-tree
    :defer 5
    :config
    (setq undo-tree-history-directory-alist '(("." . "~/.emacs.d/undo")))
    (global-undo-tree-mode 1))
#+end_src

* Tree-sitter

Auto-install tree-sitter grammars for all languages.

#+begin_src emacs-lisp
  (use-package treesit-auto
    :custom
    (treesit-auto-install 'prompt)
    :config
    (treesit-auto-add-to-auto-mode-alist 'all)
    (global-treesit-auto-mode))

#+end_src

* LSP

#+begin_src emacs-lisp
  (use-package eglot
    :ensure nil
    :hook
    ((ruby-ts-mode       . eglot-ensure)
     (go-ts-mode         . eglot-ensure)
     (typescript-ts-mode . eglot-ensure)
     (tsx-ts-mode        . eglot-ensure)
     (js-ts-mode         . eglot-ensure)
     (python-ts-mode     . eglot-ensure)
     (rust-ts-mode       . eglot-ensure)
     (c-ts-mode          . eglot-ensure)
     (c++-ts-mode        . eglot-ensure)
     (bash-ts-mode       . eglot-ensure)
     (css-ts-mode        . eglot-ensure)
     (json-ts-mode       . eglot-ensure)
     (yaml-ts-mode       . eglot-ensure)
     (toml-ts-mode       . eglot-ensure)
     (svelte-ts-mode     . eglot-ensure)
     (vue-ts-mode        . eglot-ensure))
    :custom
    (eglot-autoshutdown t)         ;; shut down server when no buffers use it
    (eglot-sync-connect nil)       ;; don't block on connection
    (eglot-extend-to-xref t)       ;; let xref use eglot for non-project files
    (eglot-code-action-indications '(eldoc-hint margin))
    (eglot-code-action-indicator "💡")
    :config
    ;; Ruby: use the nearest Gemfile directory as the eglot project root.
    ;; This ensures ruby-lsp receives the correct rootUri and workspace.
    (defun jxx/ruby-lsp-project-find (dir)
      (when (and eglot-lsp-context
                 (derived-mode-p 'ruby-ts-mode 'ruby-mode))
        (when-let* ((gemfile-dir (locate-dominating-file dir "Gemfile")))
          (cons 'jxx-ruby-project (expand-file-name gemfile-dir)))))

    (cl-defmethod project-root ((project (head jxx-ruby-project)))
      (cdr project))

    (add-to-list 'project-find-functions #'jxx/ruby-lsp-project-find)

    (defvar jxx/ruby-lsp--excluded-gems-cache (make-hash-table :test #'equal)
      "Cache of Gemfile.lock mtime to excluded gem vector.")

    (defun jxx/ruby-lsp--excluded-gems (root)
      "Return a vector of locked gem names for ROOT."
      (let* ((lockfile (expand-file-name "Gemfile.lock" root))
             (mtime (when (file-exists-p lockfile)
                      (file-attribute-modification-time (file-attributes lockfile))))
             (cached (gethash root jxx/ruby-lsp--excluded-gems-cache)))
        (if (and cached (equal (car cached) mtime))
            (cdr cached)
          (let ((default-directory root))
            (with-temp-buffer
              (if (zerop (process-file "bundle" nil t nil
                                       "exec" "ruby" "-e"
                                       "puts Bundler.locked_gems.specs.map(&:name).uniq.sort"))
                  (let* ((lines (split-string (buffer-string) "\n" t))
                         (gems (vconcat (seq-filter
                                         (lambda (line)
                                           (string-match-p "\\`[[:alnum:]_.-]+\\'" line))
                                         lines))))
                    (puthash root (cons mtime gems) jxx/ruby-lsp--excluded-gems-cache)
                    gems)
                []))))))

    (defun jxx/ruby-lsp-contact (_interactive project)
      "Build Eglot CONTACT for Ruby LSP with indexing options."
      (let* ((root (expand-file-name (project-root project)))
             (bundle-path (expand-file-name "vendor/bundle" root))
             (excluded-gems (jxx/ruby-lsp--excluded-gems root)))
        `("env" ,(format "BUNDLE_PATH=%s" bundle-path)
          "RUBY_LSP_BYPASS_TYPECHECKER=true"
          "bundle" "exec" "ruby-lsp"
          :initializationOptions
          (:formatter "none"
           :linters []
           :enabledFeatures (:diagnostics :json-false
                             :formatting :json-false
                             :onTypeFormatting :json-false)
           :indexing (:excludedGems ,excluded-gems
                      :excludedPatterns ["**/vendor/**/*"
                                         "**/node_modules/**/*"
                                         "**/tmp/**/*"
                                         "**/log/**/*"
                                         "**/public/assets/**/*"
                                         "**/sorbet/**/*"
                                         "**/spec/**/*"
                                         "**/test/**/*"])))))

    ;; Ruby LSP may register file watchers that are expensive to install on
    ;; large repos. Skip the general and add-on watchers.
    (defun jxx/eglot-skip-selected-ruby-watchers (orig-fn server method id &rest params)
      "Skip selected Ruby watcher capabilities registered via Eglot."
      (if (and (eq method 'workspace/didChangeWatchedFiles)
               (member id '("workspace-watcher"
                            "rubocop-watcher"
                            "workspace/didChangeWatchedFilesRails"
                            "workspace/didChangeWatchedFilesRuboCop")))
          nil
        (apply orig-fn server method id params)))

    (unless (advice-member-p #'jxx/eglot-skip-selected-ruby-watchers
                             'eglot-register-capability)
      (advice-add 'eglot-register-capability
                  :around
                  #'jxx/eglot-skip-selected-ruby-watchers))

    (add-to-list 'eglot-server-programs
                 '((ruby-ts-mode ruby-mode) . jxx/ruby-lsp-contact))

    ;; Re-enable font-lock after eglot managed mode activates.
    ;; gopls declares semanticTokensProvider, which causes eglot to call
    ;; (font-lock-mode -1) during setup and sometimes fail to re-enable it.
    (add-hook 'eglot-managed-mode-hook
              (lambda ()
                (unless font-lock-mode (font-lock-mode 1))
                (add-hook 'before-save-hook #'eglot-format-buffer nil t)))

    ;; Go: gopls workspace settings
    (setq-default eglot-workspace-configuration
                  '((:gopls . ((gofumpt . t)
                               (analyses . ((shadow . t)
                                            (simplifycompositelit . :json-false)))))))
  )
#+end_src

* Ruby

#+begin_src emacs-lisp
  (use-package ruby-ts-mode
    :ensure nil
    :mode (("\\.rb\\'" . ruby-ts-mode)
           ("\\.rake\\'" . ruby-ts-mode)
           ("\\.thor\\'" . ruby-ts-mode)
           ("\\.gemspec\\'" . ruby-ts-mode)
           ("\\.ru\\'" . ruby-ts-mode)
           ("\\.rabl\\'" . ruby-ts-mode)
           ("Rakefile\\'" . ruby-ts-mode)
           ("Thorfile\\'" . ruby-ts-mode)
           ("Gemfile\\'" . ruby-ts-mode)
           ("Procfile\\'" . ruby-ts-mode)
           ("Capfile\\'" . ruby-ts-mode)
           ("Vagrantfile\\'" . ruby-ts-mode))
    :interpreter ("ruby" . ruby-ts-mode)
    :config
    (add-hook 'ruby-ts-mode-hook
              (lambda ()
                (ruby-tools-mode t)))
    (define-key ruby-ts-mode-map (kbd "C-c t") 'jxx-show-ruby-tags))

  (use-package rbenv
    :config
    (setq rbenv-show-active-ruby-in-modeline nil)
    (global-rbenv-mode))

  (with-eval-after-load 'flycheck
    (defvar-local jxx/flycheck-run-sorbet-next-check nil
      "Non-nil means run Sorbet on the next Flycheck pass.")

    (defun jxx/flycheck-enable-sorbet-next-check ()
      "Enable Sorbet for the next Flycheck run in current buffer."
      (setq-local jxx/flycheck-run-sorbet-next-check t))

    (defun jxx/flycheck-run-sorbet ()
      "Manually trigger a Flycheck run that includes Sorbet once."
      (interactive)
      (jxx/flycheck-enable-sorbet-next-check)
      (flycheck-buffer))

    (defun jxx/flycheck-run-sorbet-after-save ()
      "Run Flycheck (including Sorbet) after saving a Ruby buffer."
      (when (and (derived-mode-p 'ruby-ts-mode 'ruby-mode)
                 (bound-and-true-p flycheck-mode))
        (jxx/flycheck-enable-sorbet-next-check)
        (flycheck-buffer)))

    (defun jxx/flycheck-dedup-error-list (errors)
      "Remove duplicate errors (same file, line, message) from ERRORS."
      (cl-remove-duplicates
       errors
       :test (lambda (a b)
               (and (equal (flycheck-error-filename a) (flycheck-error-filename b))
                    (equal (flycheck-error-line     a) (flycheck-error-line     b))
                    (equal (flycheck-error-message  a) (flycheck-error-message  b))))))

    ;; Filter args before overlay creation — fixes duplicate hover text in minibuffer
    (advice-add 'flycheck-display-errors :filter-args
                (lambda (args) (list (jxx/flycheck-dedup-error-list (car args)))))

    ;; Dedup the final accumulated list after all checkers finish — fixes modeline count
    (add-hook 'flycheck-after-syntax-check-hook
              (lambda ()
                (setq flycheck-current-errors
                      (jxx/flycheck-dedup-error-list flycheck-current-errors))
                (when (derived-mode-p 'ruby-ts-mode 'ruby-mode)
                  (setq-local jxx/flycheck-run-sorbet-next-check nil))))

    (flycheck-add-mode 'ruby-rubocop 'ruby-ts-mode)
    ;; Run rubocop via "bundle exec" using the rbenv shim (bypasses PATH issues)
    (add-hook 'ruby-ts-mode-hook
              (lambda ()
                (setq-local flycheck-command-wrapper-function
                            (lambda (cmd)
                              (if (string-match-p "rubocop$" (car cmd))
                                  (cons (expand-file-name "~/.rbenv/shims/bundle")
                                        (cons "exec" (cons "rubocop" (cdr cmd))))
                                cmd)))))

    (flycheck-define-checker ruby-sorbet
      "A Ruby type checker using Sorbet (srb tc)."
      :command ("bundle" "exec" "srb" "tc"
                "--no-error-count" "--no-error-sections" "--color" "never")
      :working-directory
      (lambda (_checker)
        (locate-dominating-file default-directory "sorbet"))
      :error-patterns
      ((error line-start (file-name) ":" line ": " (message) line-end))
      :modes (ruby-ts-mode ruby-mode)
      :predicate
      (lambda ()
        (and jxx/flycheck-run-sorbet-next-check
             (locate-dominating-file default-directory "sorbet"))))

    (add-to-list 'flycheck-checkers 'ruby-sorbet t)
    ;; append=t uses member-check, preventing duplicate entries on config re-eval
    (flycheck-add-next-checker 'ruby-rubocop '(t . ruby-sorbet) t)

    ;; Use bundle exec for rubocop — skip if command already starts with bundle.
    (add-hook 'ruby-ts-mode-hook
              (lambda ()
                (when (locate-dominating-file default-directory "Gemfile")
                  (add-hook 'after-save-hook #'jxx/flycheck-run-sorbet-after-save nil t)
                  (setq-local flycheck-command-wrapper-function
                              (lambda (cmd)
                                (if (equal (file-name-nondirectory (car cmd)) "bundle")
                                    cmd
                                  (append '("bundle" "exec") cmd))))))))

  ;; ERB files still use rhtml-mode
  (add-to-list 'auto-mode-alist (cons "\\.erb$" #'rhtml-mode))

  (use-package yaml-ts-mode
    :ensure nil
    :mode (("\\.yml\\'" . yaml-ts-mode)
           ("\\.yaml\\'" . yaml-ts-mode)))

  (use-package rspec-mode
    :config
    (setq rspec-use-rake-when-possible nil)
    (setq rspec-use-bundler-when-possible nil)
    (setq rspec-spec-command
          (concat (expand-file-name "~/.rbenv/shims/bundle") " exec rspec")))

  (use-package ruby-tools)

  ;;; FUNCTIONS
  (defun ruby-open-spec-other-buffer ()
    (interactive)
    (when (featurep 'rspec-mode)
      (let ((source-buffer (current-buffer))
            (other-buffer (progn
                            (rspec-toggle-spec-and-target)
                            (current-buffer))))
        (switch-to-buffer source-buffer)
        (pop-to-buffer other-buffer))))

  (defun jxx-show-ruby-tags ()
    (interactive)
    (occur "^\\s-*\\\(class \\\|module \\\|def \\\|[^:]include \\\|private\\b\\\|protected\\b\\\)"))

  (defun goto-match-paren (arg)
    "Go to the matching  if on (){}[], similar to vi style of % "
    (interactive "p")
    ;; first, check for "outside of bracket" positions expected by forward-sexp, etc
    (cond ((looking-at "[\[\(\{]") (forward-sexp))
          ((looking-back "[\]\)\}]" 1) (backward-sexp))
          ;; now, try to succeed from inside of a bracket
          ((looking-at "[\]\)\}]") (forward-char) (backward-sexp))
          ((looking-back "[\[\(\{]" 1) (backward-char) (forward-sexp))
          (t nil)))

  (defun goto-matching-ruby-block (arg)
    (cond
     ;; are we at an end keyword?
     ((equal (current-word) "end")
      (ruby-beginning-of-block))

     ;; or are we at a keyword itself?
     ((string-match (current-word) "\\(for\\|while\\|until\\|if\\|class\\|module\\|case\\|unless\\|def\\|begin\\|do\\|context\\|describe\\it\\)")
      (ruby-end-of-block))))

  (defun dispatch-goto-matching (arg)
    (interactive "p")

    (if (or
         (looking-at "[\[\(\{]")
         (looking-at "[\]\)\}]")
         (looking-back "[\[\(\{]" 1)
         (looking-back "[\]\)\}]" 1))

        (goto-match-paren arg)

      (when (or (eq major-mode 'ruby-mode) (eq major-mode 'ruby-ts-mode))
        (goto-matching-ruby-block arg))))

  (global-set-key "\M--" 'dispatch-goto-matching)
#+end_src

* Rust

#+begin_src emacs-lisp
  (use-package cargo)
  (use-package flycheck-rust)

  (use-package rustic
    :ensure
    :config
    ;; comment to disable rustfmt on save
    (setq rustic-format-on-save t)
    (add-hook 'rustic-mode-hook 'rk/rustic-mode-hook))

  (defun rk/rustic-mode-hook ()
    ;; so that run C-c C-c C-r works without having to confirm, but don't try to
    ;; save rust buffers that are not file visiting. Once
    ;; https://github.com/brotzeit/rustic/issues/253 has been resolved this should
    ;; no longer be necessary.
    (when buffer-file-name
      (setq-local buffer-save-without-query t)))
#+end_src
* Javascript

JavaScript is configured in the Modes section using js-ts-mode.

#+begin_src emacs-lisp
  (use-package typescript-mode
    :mode
    (("\\.ts\\'" . tsx-ts-mode)
     ("\\.tsx\\'" . tsx-ts-mode))

    :interpreter "ts-node"

    :custom
    (js-indent-level 2)
    (js-jsx-indent-level 2)
    (evil-shift-width 2)
    (truncate-lines t)
    (add-node-modules-path)

    :hook
    (tsx-ts-mode . (lambda () (indent-tabs-mode -1))))
#+end_src

* Mastodon
#+begin_src emacs-lisp
  (use-package mastodon
    :config
    (setq mastodon-instance-url "https://nein.club"
          mastodon-active-user "julienxx"
          mastodon-auth-source-file "~/.netrc"))
#+end_src

* Browsing the web and gopher/gemini

Use elpher for all things gopher/gemini

#+begin_src emacs-lisp
  (use-package elpher)
#+end_src

Browse everything with EWW

#+begin_src emacs-lisp
  ;; (setq browse-url-browser-function 'eww-browse-url)
#+end_src

Open gopher and gemini links in elpher

#+begin_src emacs-lisp
  (advice-add 'eww-browse-url :around 'asc:eww-browse-url)

  (defun asc:eww-browse-url (original url &optional new-window)
    "Handle gemini links."
    (cond ((string-match-p "\\`\\(gemini\\|gopher\\)://" url)
           (require 'elpher)
           (elpher-go url))
          (t (funcall original url new-window))))
#+end_src

* RSS

#+begin_src emacs-lisp
  (use-package elfeed-protocol)

  (use-package elfeed
    :commands (elfeed)
    :config
    (setq elfeed-db-directory "~/.elfeed")
    (setq elfeed-enclosure-default-dir (expand-file-name "~/Downloads"))
    (setq elfeed-use-curl t)
    (elfeed-set-timeout 3600)
    (setq elfeed-curl-extra-arguments '("--insecure"))
    (setq-default elfeed-search-filter "+unread ")
    (advice-add #'elfeed-insert-html
                :around
                (lambda (fun &rest r)
                  (let ((shr-use-fonts nil))
                    (apply fun r))))
    (require 'elfeed-protocol)
    (let ((freshrss-url "https://rss.typed-hole.org/p/api/fever.php"))
      (setq elfeed-feeds (list
                          (list "fever+https://julien@rss.typed-hole.org"
                                :api-url freshrss-url
                                :password (jxx-netrc-get freshrss-url "password")))))
    (setq elfeed-protocol-fever-update-unread-only t)
    (elfeed-protocol-enable))
#+end_src

Use B to browse article in gui browser instead of eww.

#+begin_src emacs-lisp
  (defun jxx/elfeed-show-visit-gui ()
    "Wrapper for elfeed-show-visit to use gui browser instead of eww"
    (interactive)
    (let ((browse-url-generic-program "/usr/bin/xdg-open"))
      (elfeed-show-visit t)))

  (add-hook 'elfeed-show-mode-hook
            (lambda () (setq-local browse-url-browser-function #'eww-browse-url)))

  (define-key elfeed-show-mode-map (kbd "B") 'jxx/elfeed-show-visit-gui)
  (define-key elfeed-show-mode-map (kbd "b") 'elfeed-show-visit)

  (defun jxx/elfeed-purge (&optional days)
    "Delete entries older than DAYS (default 90) that are not starred."
    (interactive "P")
    (let* ((days (or days 90))
           (cutoff (- (float-time) (* days 24 60 60)))
           (to-delete (cl-loop for entry being the hash-values of elfeed-db-entries
                               when (and (< (elfeed-entry-date entry) cutoff)
                                         (not (elfeed-tagged-p 'star entry)))
                               collect entry)))
      (elfeed-db-delete to-delete)
      (message "Purged %d entries older than %d days" (length to-delete) days)))
#+end_src

* LLM

#+begin_src emacs-lisp
  (use-package copilot
    :hook (prog-mode . copilot-mode)
    :bind (:map copilot-completion-map
                ("<tab>" . 'copilot-accept-completion)
                ("TAB" . 'copilot-accept-completion)
                ("C-TAB" . 'copilot-accept-completion-by-word)
                ("C-<tab>" . 'copilot-accept-completion-by-word)
                ("C-n" . 'copilot-next-completion)
                ("C-p" . 'copilot-previous-completion))

    :config
    (add-to-list 'copilot-indentation-alist '(prog-mode 2))
    (add-to-list 'copilot-indentation-alist '(org-mode 2))
    (add-to-list 'copilot-indentation-alist '(text-mode 2))
    (add-to-list 'copilot-indentation-alist '(clojure-mode 2))
    (add-to-list 'copilot-indentation-alist '(emacs-lisp-mode 2)))
#+end_src

#+begin_src emacs-lisp
  (use-package agent-shell
    :config
    (setq agent-shell-anthropic-authentication
          (agent-shell-anthropic-make-authentication
           :login t)))
#+end_src

* EditorConfig

#+begin_src emacs-lisp
  (use-package editorconfig
    :config
    (editorconfig-mode 1))
#+end_src
